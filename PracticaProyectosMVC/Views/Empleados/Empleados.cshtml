@model IEnumerable<PracticaProyectosMVC.Models.ViewModels.EmpleadosViewModel>

@{
    ViewBag.Title = "Empleados";
}

<h2>Empleados</h2>
@foreach (var item in Model)
{
    <div>
        @item.nombre, @item.dni
    </div>
}